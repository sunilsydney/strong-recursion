namespace StrongRecursion.StateMachine
{
    /// <summary>
    /// Possbile transitions
    /// </summary>
    public enum Transitions
    {
        If = 0,
        Then0,
        ElseIf,
        Then1,
        Else,
        Then2,
        Finish
    }
}